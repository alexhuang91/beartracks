= form_tag package_path(@package), :method=>'put'  do

  = render :partial => "form"

  .options
    %br
    = submit_tag "Update"
    %br
    %br

.options 
  = link_to "Cancel", package_path(@package)